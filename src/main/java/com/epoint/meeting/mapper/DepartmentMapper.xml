<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.epoint.meeting.mapper.DepartmentMapper">

    <select id="getDeptById" resultType="com.epoint.meeting.model.Department">
        select * from department where departmentid = #{id}
    </select>

</mapper>
